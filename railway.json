{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
        "buildCommand": "npm ci --production && npm run prisma:generate"
  },
  "deploy": {
    "rootDirectory": "backend_bala",
    "startCommand": "node index.js",
    "restartPolicyMaxRetries": 10
  }
}
